1.未解决的问题,:
1 顶部横向滑动条模糊原因
2 懒加载 圆圈变大
3 
学到的知识:
1.布局的时候显示图片的每一项为li标签
在改为路由跳时(router-link),页面样式
会发生无效,因为标签变了!
如何渲染成指定的标签 用属性tag='li',
还有解决的办法就是给标签加class 来指定样式
2. vertical-align:middle;样式可以去掉图片
自带的3px大厚度
阴影用法的熟悉:box-shadow:0 0 0 xxrem;
透明度background-color:rgba();



注意事项:
1 router-link to='' 改造路由时,注区别一级路由和子路由
一级路由是显示整个页面的变换,子路由是一个页面局部变化 
2 路由路径配置时,需要动态添加 注意双引号运用
3,v-for 使用,注意要给循环的元素绑定key
4. 注意注册路由的时候要用动态的添加 :id 
